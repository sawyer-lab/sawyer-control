services:
  bridge:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    network_mode: host
    environment:
      ROBOT_IP: ${ROBOT_IP:?ROBOT_IP is required}
      HOST_IP: ${HOST_IP:?HOST_IP is required}
    restart: unless-stopped
